#include<stdio.h>

int main()
{
    int length = 2;
    int width = 3;
    /*
    int length,width;
    printf("Enter length :");
    scanf("%d",&length);

    printf("Enter width :");
    scanf("%d",&width);
    */
    
    printf("There is Area of Rectangle = %d" ,length*width);

    return 0;
}