#include<stdio.h>
#include<math.h>
int main()
{
    // NOTE : Single variable on the LHS
    //int a = 1, b = 2;
    //int sum = a + b;

    /*
    Invalid Opertor
    int x, y = a * b;
    b + c = a;
    a = bc;
    a = b^c;
    */
   int power = pow(b,c);
   printf("%d",power);

    return 0;
}