#include<stdio.h>
#include<string.h>

// Q.3.-> Find the salted form of a password entered by user if the salt is "123" & added at the end.

int main()
{   
    char password[ 100 ];
    scanf("%s",password);
    return 0;
}